/**
 *  ? Qs3. Create an object Person with their name, age and city. Edit their city’s original value to change it to “New York”. Add a new property country and set it to the United States.
 */

const person = {
    name : "anuptachamo",
    age : 23,
    city : "Bhaktapur"
}

console.log(person)

// changing city value to New York
console.log(person.city = "New York")
console.log(person)

// adding new property country to United States
console.log(person.country = "united States")
console.log(person)