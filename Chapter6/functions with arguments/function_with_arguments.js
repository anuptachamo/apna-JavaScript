/**
 *  ? Functions with arguments
 *      *Values we pass to the function
 *      
 *      *Functions in JavaScript can take parameters, also known as arguments. 
 *      *These parameters allow you to pass values into the function when you call it.
 *      
 *      function funcName (arg1, arg2..){
 *              do something
 *      }
 */

function printInfo(fullname, age){  //fullname and age  are variable
        console.log(`${fullname} age is ${age}`)
}


printInfo("Anup Tachamo", 23);
printInfo("Sanil koju")