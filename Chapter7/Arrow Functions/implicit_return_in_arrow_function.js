/**
 *  ?Arrow Function
 *      *Implicit return
 *          if their is only a single  work in arrow function, in that case we can use implicit return
 *          
 *          const func = (arg1, arg2..) => {value}
 */

// example for implicit return

const mul = (a, b) => (a * b);  //(a * b) => In this case here is  parenthesis is optional

console.log(mul(2,3)) 