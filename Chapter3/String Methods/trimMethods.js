/**
 *  ? String Methods
 *      let msg = "Hello";
 *      
 *      str.trim()
 *      *trim whitespaces from both ends of string and returns a new one.
 *          let msg = " Hello   ";
 *          msg.trim();
 */


// example for trim methods
let msg = " He llo   ";
console.log(msg.trim());

// another example for trim methods
let password = prompt("set your password");
let newPass = password.trim();
console.log(password)
console.log(newPass) //trim methods are use simply to remove a spaces
