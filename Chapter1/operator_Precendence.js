/**
 * ? Operator Precedence
 *      *This is the general order of solving an expression.
 *          (),
 *          **,
            *, /, %,
            +, -
 */
a = (2 + 1) * 3;
console.log(a);

b = 3/1 + 2**2;
console.log(b);